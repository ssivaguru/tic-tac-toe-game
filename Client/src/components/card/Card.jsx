import './card.scss';

const Card = (props) => {
    console.log(props.width)
    
    return (
        <div style={{width: 300, height: 300}} className="card-container">
            yolo
        </div>
    )
}

export default Card;